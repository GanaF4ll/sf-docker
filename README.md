# Lancer le projet
Dans le terminal, utiliser la commande: 
docker-compose up -d


# Le service wordpress est défini sur le port 24
# Le service phpmyadmin est défini sur le port 8888


L'image Mariadb est utilisé parce que mysql n'était pas compatible avec le système arm64
